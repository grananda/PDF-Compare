name: Build and Release

on:
  push:
    branches: ['**']
  workflow_dispatch:

jobs:
  check-versions:
    runs-on: ubuntu-latest
    outputs:
      npm_version: ${{ steps.get_npm_version.outputs.version }}
      should_publish_npm: ${{ steps.check_npm_tag.outputs.should_publish }}
      should_release: ${{ steps.check_release_tag.outputs.should_release }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Get npm version from package.json
        id: get_npm_version
        run: |
          VERSION=$(node -p "require('./package.json').version")
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "NPM version: $VERSION"

      - name: Check if npm tag exists
        id: check_npm_tag
        run: |
          if git rev-parse "npm-v${{ steps.get_npm_version.outputs.version }}" >/dev/null 2>&1; then
            echo "should_publish=false" >> $GITHUB_OUTPUT
            echo "Tag npm-v${{ steps.get_npm_version.outputs.version }} already exists"
          else
            if [[ "${{ github.event_name }}" == "push" && "${{ github.ref }}" == "refs/heads/main" ]]; then
              echo "should_publish=true" >> $GITHUB_OUTPUT
              echo "Tag npm-v${{ steps.get_npm_version.outputs.version }} does not exist - will publish to npm"
            else
              echo "should_publish=false" >> $GITHUB_OUTPUT
              echo "Not on main branch push - skipping npm publish"
            fi
          fi

      - name: Check if release tag exists
        id: check_release_tag
        run: |
          if git rev-parse "v${{ steps.get_npm_version.outputs.version }}" >/dev/null 2>&1; then
            echo "should_release=false" >> $GITHUB_OUTPUT
            echo "Tag v${{ steps.get_npm_version.outputs.version }} already exists"
          else
            if [[ "${{ github.event_name }}" == "push" && "${{ github.ref }}" == "refs/heads/main" ]]; then
              echo "should_release=true" >> $GITHUB_OUTPUT
              echo "Tag v${{ steps.get_npm_version.outputs.version }} does not exist - will create release"
            else
              echo "should_release=false" >> $GITHUB_OUTPUT
              echo "Not on main branch push - skipping release"
            fi
          fi

  verify-npm-token:
    runs-on: ubuntu-latest
    steps:
      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '22'
          registry-url: 'https://registry.npmjs.org'

      - name: Verify npm token
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          echo "Verifying npm authentication..."
          npm whoami
          echo "npm token is valid"

  publish-npm:
    needs: check-versions
    runs-on: ubuntu-latest
    if: needs.check-versions.outputs.should_publish_npm == 'true'

    permissions:
      contents: write
      id-token: write  # Required for OIDC trusted publishing

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '22'
          registry-url: 'https://registry.npmjs.org'

      - name: Publish to npm
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          VERSION="${{ needs.check-versions.outputs.npm_version }}"

          # Determine npm tag based on version
          if [[ "$VERSION" == *"rc"* ]] || [[ "$VERSION" == *"alpha"* ]] || [[ "$VERSION" == *"beta"* ]]; then
            NPM_TAG="next"
          else
            NPM_TAG="latest"
          fi

          echo "Publishing version $VERSION with tag $NPM_TAG"
          npm publish --provenance --access public --tag "$NPM_TAG"

      - name: Create npm version tag
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          VERSION="npm-v${{ needs.check-versions.outputs.npm_version }}"
          git tag "$VERSION"
          git push origin "$VERSION"

  release:
    needs: check-versions
    runs-on: ubuntu-latest
    if: needs.check-versions.outputs.should_release == 'true'

    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '22'

      - name: Pack tarball
        run: |
          mkdir -p dist   # npm pack does not create the destination directory
          npm pack --pack-destination dist
          echo "Packed:"
          ls -lh dist/

      - name: Create tag and release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          VERSION="v${{ needs.check-versions.outputs.npm_version }}"

          git tag "$VERSION"
          git push origin "$VERSION"

          gh release create "$VERSION" \
            dist/*.tgz \
            --title "$VERSION" \
            --generate-notes
